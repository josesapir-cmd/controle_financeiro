node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env
.env.local
.env*.local
next-env.d.ts
*.tsbuildinfo
# itemIds e cadastro de contrapartes ficam fora do versionamento
/data/*.json
!/data/.gitkeep

# Backups de classificacao: dado financeiro, fica so na maquina
/backups/

# Remuneracao pessoal: o CSV que alimenta `scripts/receita.mjs` nunca entra no
# repositorio, nem por engano de `git add -A`.
/receitas*.csv
